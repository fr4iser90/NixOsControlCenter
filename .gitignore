# Log files
# Exclude all log files to avoid tracking transient output from the application.
*.log
nixos_control_center.log
system-config.nix.backup.*
build-gaming.sh
# Virtual environments and environment variables
# Exclude virtual environment directories and local .env files for environment-specific configurations.
venv/
.env/

# System-specific files
# Files created by macOS or Windows that have no relevance to the project.
.DS_Store
Thumbs.db

# IDE and editor settings
# Exclude configuration files generated by editors such as VSCode, PyCharm, or Vim.
.vscode/
.idea/
*.swp
*.swo

# Project setup and boilerplate scripts
# These scripts are meant for local development and should not be included in the repository.
setup_project.sh
structure
secret/
secrets/
*.secret
hardware-configuration.nix

# Generated documentation
# If you generate HTML or other files during documentation generation, exclude them.
docs/_build/
*.rst~

# Temporary files
# Temporary files created during development or by certain tools.
tmp/
*.tmp
*.bak
*.old

# Build artifacts
# If you build distributions (e.g., wheels, eggs), exclude these directories/files.
dist/
build/
*.egg-info/

# Debugging files
# Core dumps or debugging output files should not be tracked.
core.*
*.stackdump

# NixOS Control Center specific
generation_names.json
logs/

# Nix specific
result
result-*
.direnv/
.envrc

# Python specific (zusätzlich)
*.pyc
__pycache__/
.python-version
.coverage
htmlcov/
.tox/
.nox/
.pytest_cache/
.mypy_cache/
.pyre/
.pytype/

# Local development
*.sqlite
*.db
config.local.yaml
/nixos/local/*