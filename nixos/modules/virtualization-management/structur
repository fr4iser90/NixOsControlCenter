virtualization-management/
├── base                          # Alles Grundlegende
│   ├── config                    # Basiskonfigurationen
│   │   ├── default.nix          # Hauptkonfiguration
│   │   ├── network.nix          # Netzwerkeinstellungen
│   │   └── storage.nix          # Speichereinstellungen
│   └── system                    # Systemanforderungen
│       ├── checks.nix           # Systemchecks
│       └── packages.nix         # Benötigte Pakete
│
├── tools                         # Werkzeuge & Hilfsprogramme
│   ├── iso-tools                # ISO-Verwaltung
│   │   ├── downloader.nix      # Download-Funktionen
│   │   └── validator.nix       # Prüffunktionen
│   ├── network-tools            # Netzwerkwerkzeuge
│   │   ├── port-manager.nix    # Port-Verwaltung
│   │   └── bridge-manager.nix  # Netzwerkbrücken
│   └── storage-tools           # Speicherwerkzeuge
│       └── disk-manager.nix    # Festplattenverwaltung
│
├── vm-manager                    # VM-Verwaltung
│   ├── templates                # VM-Vorlagen
│   │   ├── desktop.nix         # Desktop-VMs
│   │   ├── server.nix          # Server-VMs
│   │   └── test.nix            # Test-VMs
│   └── drivers                  # VM-Treiber
│       ├── qemu.nix            # QEMU-Konfiguration
│       └── virtualbox.nix      # VirtualBox-Konfiguration
│
├── container-manager             # Container-Verwaltung
│   ├── docker                   # Docker-Spezifisch
│   │   └── config.nix           # Docker-Konfiguration
│   └── podman                   # Podman-Spezifisch
│       └── config.nix           # Podman-Konfiguration
│
└── lib                          # Gemeinsame Funktionen
    ├── types.nix               # Typdefinitionen
    ├── utils.nix              # Hilfsfunktionen
    └── constants.nix          # Konstanten