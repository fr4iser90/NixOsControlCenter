# PUID und PGID (typisch für LinuxServer.io-Container)
PUID=1000
PGID=1000

# Zeitzone
TZ=Europe/Berlin

# Zusätzliche Konfigurationen sind bei ddclient meist in /config/ddclient.conf hinterlegt,
# dort gibst du deine Cloudflare API-Daten ein, DynDNS-Intervalle usw.

# Falls ddclient-Container selbst etwas für Cloudflare braucht, kannst du hier ENV-Variablen definieren.
# Beachte aber, dass ddclient üblicherweise direkt aus /config/ddclient.conf konfiguriert wird.
